# /config/pkg/water.yaml

template:
#
  - sensor:
#
    - name: T8 Water Level
      unique_id: t8_water_level
      state: >-
        {% set a = states('sensor.t8_ultrasonic')|float(-1) %}
        {% set b = 0.40 %}
        {{ (b - a)|round(2) }}
      state_class: measurement
      unit_of_measurement: "m"
      icon: "mdi:leak"

# EOF
