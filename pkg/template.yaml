# /config/pkg/template.yaml

template:
#
  - sensor:
#
    - name: SR1 Water Level
      unique_id: sr1_water_level
      state: >-
        {% set a = states('sensor.t8_ultrasonic')|float(0) %}
        {% set b = 50 %}
        {% if a > 0 %}
          {{ ((b - a) / 100)|round(2) }}
        {% endif %}
      state_class: measurement
      unit_of_measurement: "m"
      icon: "mdi:leak"

# EOF
